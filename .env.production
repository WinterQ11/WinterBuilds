# Production environment defaults
# For full-stack deployments (Render / containers), keep empty to use relative /api routes.
# For static CDN deployments (e.g. Cloudflare Pages), configure your active backend API URL:
# VITE_API_BASE_URL=https://winterbuilds.onrender.com
VITE_API_BASE_URL=
